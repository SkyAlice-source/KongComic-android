# 收藏书签星星化 + 应用图标概念生成

## 问题 / 需求
1. 用户把收藏角标参考图发过来：圆角矩形书签、底部 V-cut、中间白色五角星，并要求跟随主题色变化。
2. 用户想要新的应用图标：几本叠起来的书，最上方是魔法六角星芒。

## 改动

### 1. 收藏书签改成星星书签
- 文件：`lib/components/comic.dart`
- `_buildFavoriteBadge` 尺寸从 20×38 调整为 **20×30**，更贴近参考图比例。
- `_BookmarkRibbonPainter` 重画：
  - 顶部圆角、底部 V 形缺口。
  - 主题色渐变填充（顶部 `colorScheme.primary` → 底部略深），跟随主题色自动变化。
  - 黑色投影、外发光白边、实白描边、顶部内高光，确保任意封面都可见。
  - 中心绘制**白色五角星**。
- brief 模式右侧描述 chips 顶部避让从 42 降到 **34**（书签变矮了）。
- detailed/brief 模式都贴在封面右上角（`right:0, top:0`）。

### 2. 应用图标概念生成
- 文件：`app_icon_concepts/A_clean_mobile_app_icon_design_2026-07-16T19-37-17.png`
- 描述：三本书叠起，最上方一本翻开，上方悬浮发光的魔法六角星芒，现代扁平矢量风格。

## 验证
- `flutter analyze` 零问题。
- 构建 release APK 并安装到 `emulator-5554`。
- 截图验证：
  - **收藏页**：封面右上角蓝色星星书签清晰可见，chips 避让正确。
  - **历史页**：detailed 卡片封面、标题、描述、底部进度条均正常，星星书签也清晰。
- 所有改动仍只留在本地工作树，未 commit/tag/push。
